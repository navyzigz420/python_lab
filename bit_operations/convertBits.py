bits = '110'


def turnBitsIntoInteger(listOfBits):
    valore = 0
    lunghezza = len(listOfBits)
    for x in range(lunghezza):
        if listOfBits[x] != '0' and listOfBits[x] != '1':
            raise Exception('Not a combinations of bits!')
        valore = valore + 2**(lunghezza -1 - x) * int(listOfBits[x])
        
    if valore != int(listOfBits,2):
        raise Exception('Porca l\'oca. {} diverso da {}!'.format(valore, int(listOfBits,2)))
        
    return valore

   
#print(str(valore))

print(turnBitsIntoInteger(bits))


path = 'C:\\Users\\admin\\Documents\\GitHub\\python_lab\\bit_operations\\files\\binaries.txt'

path2 = 'C:\\Users\\admin\\Documents\\GitHub\\python_lab\\bit_operations\\files\\integers.txt'


# =============================================================================
# integers = ""
# with open(path,'r') as f:
#     for line in f:
#         integers = integers + str(turnBitsIntoInteger(line.rstrip())) + "\n" 
#         
# with open(path2,'w') as f:
#     f.write(integers)
# =============================================================================
    

with open(path,'r') as f:
    
    with open(path2,'w') as f2:
        
        for line in f: 
            f2.write(str(turnBitsIntoInteger(line.rstrip())) + "\n") 
        





















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































